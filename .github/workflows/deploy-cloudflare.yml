name: Deploy RiseLooter to Cloudflare

on:
  push:
    branches: [main]
    paths:
      - 'worker.js'
      - 'wrangler.jsonc'
      - 'creator-cache-v23.js'
      - 'creator-hd.css'
      - 'index.html'
      - 'assets/creator/**'
      - '.github/workflows/deploy-cloudflare.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: riselooter-cloudflare-production
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Verify Cloudflare credential type
        shell: bash
        run: |
          set +e
          user_code=$(curl -sS -o /tmp/userverify.json -w '%{http_code}' -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" https://api.cloudflare.com/client/v4/user/tokens/verify)
          account_code=$(curl -sS -o /tmp/accountverify.json -w '%{http_code}' -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" "https://api.cloudflare.com/client/v4/accounts/$CLOUDFLARE_ACCOUNT_ID/tokens/verify")
          user_ok=$(python3 -c "import json; print(str(json.load(open('/tmp/userverify.json')).get('success',False)).lower())" 2>/dev/null || echo false)
          account_ok=$(python3 -c "import json; print(str(json.load(open('/tmp/accountverify.json')).get('success',False)).lower())" 2>/dev/null || echo false)
          printf 'user_verify_http=%s\nuser_verify_success=%s\naccount_verify_http=%s\naccount_verify_success=%s\n' "$user_code" "$user_ok" "$account_code" "$account_ok" > cloudflare-auth-diagnostic.txt
          exit 0
      - name: Deploy Worker and static assets
        id: deploy
        shell: bash
        run: |
          set +e
          npx --yes wrangler@4 deploy > /tmp/wrangler-deploy.log 2>&1
          code=$?
          set -e
          sed -E 's/cfat_[A-Za-z0-9_-]+/[REDACTED_TOKEN]/g' /tmp/wrangler-deploy.log | tail -n 120 > cloudflare-deploy-log.txt
          if [ "$code" -eq 0 ]; then echo 'outcome=success' >> "$GITHUB_OUTPUT"; else echo 'outcome=failure' >> "$GITHUB_OUTPUT"; fi
          exit 0
      - name: Record Cloudflare deployment result
        if: always()
        env:
          HAS_CF_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN != '' }}
          HAS_CF_ACCOUNT: ${{ secrets.CLOUDFLARE_ACCOUNT_ID != '' }}
        shell: bash
        run: |
          set -euo pipefail
          printf 'status=%s\nsource_commit=%s\ncreator_version=dedicated-fullbody-v23\nasset_cache=fullbody23\napi_token_present=%s\naccount_id_present=%s\n' '${{ steps.deploy.outputs.outcome }}' "$GITHUB_SHA" "$HAS_CF_TOKEN" "$HAS_CF_ACCOUNT" > cloudflare-deploy-status.txt
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add cloudflare-deploy-status.txt cloudflare-deploy-log.txt cloudflare-auth-diagnostic.txt
          git diff --cached --quiet || (git commit -m 'Record Cloudflare deployment diagnostics' && git push origin HEAD:main)
      - name: Fail when deployment failed
        if: steps.deploy.outputs.outcome != 'success'
        run: exit 1
